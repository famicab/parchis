# URL del backend (Socket.IO). Vite la inyecta en BUILD, así que debe estar definida
# en el entorno de Vercel ANTES de compilar.
# Local:      http://localhost:3001
# Producción: la URL pública del backend en Render, p. ej. https://parchis-server.onrender.com
VITE_SERVER_URL=http://localhost:3001
